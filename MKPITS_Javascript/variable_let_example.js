//let introduces block-scoped variables. It’s commonly used for variables that may change their value.
let x = 10;
let y = 20;

//let x = 30;//not accept  because variable is already declared with let keyword
//let c = 10;//accept as it's a different variable than the one above

// In "let" similar element can not accept but similar value is accept. */

let z = x + y;
console.log("Addition of z :" + z);
