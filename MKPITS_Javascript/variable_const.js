//variable cannot be re-declared(means we cannot write 'const' again) and updated(means we cannot write new rollNo value i,e 2).A block scope variable.
//const means 'constant value i,e 3.14'.
const PI = 3.14;
console.log(PI);

//const a;
//it gives error because in 'const' we have to give some value to the variable.

//console.log(a);//undefined because it is not initialized yet. 

const b = 1;
console.log(b);