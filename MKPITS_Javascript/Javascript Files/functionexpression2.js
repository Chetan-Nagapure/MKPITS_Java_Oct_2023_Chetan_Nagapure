let calculator = function(s, e) {
    let total1 = s;
    let total2 = e;

    function contain(a, b) {
        let sum = a + b;
        return sum ;
    }

    return contain(total1, total2);
}

console.log(calculator(5, 20));
