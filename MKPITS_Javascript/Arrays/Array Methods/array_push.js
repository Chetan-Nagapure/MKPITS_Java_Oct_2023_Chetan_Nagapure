//Adding new elements such as some numbers and some string values to the end of the array using the push() method.
let numArr=[10,20,30,40,50];
console.log(numArr);

let numArr1= numArr.push(60);
console.log(numArr);
