//A for-of loop is used to loop through the values of an iterable object (such as an array).
const arr=[1,2,3,4,5];
for(let value of arr)
{
    console.log(value);//this will print each element in the array
}